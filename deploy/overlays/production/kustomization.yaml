apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

namePrefix: prod-
namespace: plavenky-production

commonLabels:
  app.kubernetes.io/environment: production

patches:
  - path: patch-ingress.yaml
  - path: patch-deployment.yaml
