apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

namePrefix: staging-
namespace: plavenky-staging

commonLabels:
  app.kubernetes.io/environment: staging

patches:
  - path: patch-ingress.yaml
  - path: patch-deployment.yaml
  - path: patch-configmap.yaml
